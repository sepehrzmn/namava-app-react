import { Swiper, SwiperSlide } from "swiper/react";

const Latest = ({ data }) => {
    return (
        <div className="latest">
            <div>
                <Swiper>{}</Swiper>
            </div>
        </div>
    );
};

export default Latest;
